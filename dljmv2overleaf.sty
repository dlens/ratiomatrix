\ProvidesPackage{dljmv2overleaf}
\DeclareOption{fullpage}{
  \AtEndOfPackage{\RequirePackage{fullpage}}
}

\DeclareOption{design}{
	\AtEndOfPackage{
		\RequirePackage{xltxtra}
        \setmainfont[    Extension = .otf,
           UprightFont = *-Reg,
              BoldFont = *-Bold,
            ItalicFont = *-Reg-It,
        BoldItalicFont = *-Bold-It,
        ]{Proxima_Nova}

		\pagestyle{fancyplain}
		\fancyhead{}
		\fancyhead[R]{\includegraphics[height=0.25in]{logo}}
		\renewcommand{\headrulewidth}{0pt}
		\fancyheadoffset[R]{0.0in}
		\RequirePackage{setspace}
		\onehalfspacing
%		\let\oldmaketitle\maketitle
%		\renewcommand{\maketitle}{\oldmaketitle \thispagestyle{fancy}}
		\RequirePackage[headsep=0.65in, top=1.0in, bottom=1.0in, left=1.25in, right=1.25in]{geometry}
	}
}

\DeclareOption{journal}{
 \AtEndOfPackage{\RequirePackage[margin=.55in, paperwidth=6in, paperheight=9in]{geometry}}
}
\ProcessOptions\relax
\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{titling}
\usepackage{dsfont}
%\usepackage{hyperref}
\usepackage{graphicx}
\usepackage{boxedminipage}
\usepackage{color}
\usepackage[bottom]{footmisc}
\usepackage{booktabs, multicol, multirow}
\usepackage{todonotes}
\usepackage{fancyhdr}
%\usepackage{nameref}
\usepackage{framed}

\pagestyle{fancy}
\fancyhead{}
\fancyhead[CO]{\thedate}
\fancyhead[CE]{\thetitle}
\fancyhead[LE,RO]{\thepage}

\newcommand{\R}{\textsc{R}}
\ifdefined \theorem \else \newtheorem{theorem}{Theorem} \fi
\ifdefined \corollary \else \newtheorem{corollary}{Corollary} \fi
\ifdefined \context \else \newtheorem{context}{Context} \fi
\ifdefined \solution \else \newtheorem{solution}{Solution} \fi
%\ifdefined \problem \else \newtheorem{problem}{Problem} \fi
\ifdefined \definition \else {\theoremstyle{definition} \newtheorem{definition}{Definition}} \fi
\ifdefined \goalstar \else {\theoremstyle{definition} \newtheorem*{goalstar}{Goal}} \fi
\ifdefined \examplestar \else {\theoremstyle{remark} \newtheorem*{example*}{Example}} \fi
\ifdefined \remarkstar \else {\theoremstyle{remark} \newtheorem*{remarkstar}{Example}} \fi
\ifdefined \note \else \newtheorem{note}{Note} \fi

\providecommand{\fixme}[1]{\todo[inline]{#1}}
\providecommand*{\currentname}{\@currentlabelname}
\providecommand{\keywords}[1]{\textbf{\emph{Keywords:}}} 

\providecommand{\qcode}[1]{`\texttt{#1}'}
\providecommand{\code}[1]{\texttt{#1}}
\providecommand{\url}[1]{\textcolor{blue}{\underline{#1}}}
\ifdefined \definitionstar \else {\theoremstyle{definition}\newtheorem*{definitionstar}{Definition}} \fi
\ifdefined \notestar \else {\theoremstyle{definition}\newtheorem*{notestar}{Note}} \fi
{\theoremstyle{definition} \newtheorem{example}{Example}}
{\theoremstyle{definition} \newtheorem{remark}{Remark}}
{\theoremstyle{definition} \newtheorem{question}{Question}}
{\theoremstyle{definition} \newtheorem{problem}{Problem}}

